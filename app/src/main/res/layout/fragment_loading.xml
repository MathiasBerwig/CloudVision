<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:htext="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="io.github.mathiasberwig.cloudvision.presentation.fragment.LoadingFragment">

    <!-- Loading GIF -->
    <pl.droidsonroids.gif.GifImageView
        android:id="@+id/gif_loading"
        android:layout_width="256dp"
        android:layout_height="256dp"
        android:layout_gravity="center"
        android:layout_marginBottom="35dp"
        android:src="@drawable/ic_loading_anim_256dp" />

    <!-- Distracting Text -->
    <com.hanks.htextview.HTextView
        android:id="@+id/text_loading"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginLeft="25dp"
        android:layout_marginRight="25dp"
        android:layout_marginTop="90dp"
        android:labelFor="@id/gif_loading"
        android:textAlignment="center"
        android:textColor="@color/colorPrimary"
        android:textSize="18sp"
        htext:animateType="scale"
        tools:text="Placeholder Text Test"
        android:textStyle="bold" />

</FrameLayout>
